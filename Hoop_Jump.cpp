#include <iostream>
using namespace std;
int main()
{
    int t = 0, n = 0;
    cin >> t;
    while (t--)
    {
        cin >> n;
        cout << (n + 1) / 2 << '\n';
    }
    return 0;
}
//here i learn every time loops not necessary find some pattren and think out of the box
//i written this code using one nested loop and tones of if condition but solution is so simple